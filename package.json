{
  "name": "typescript",
  "version": "1.0.0",
  "description": "- 타입이 정의된 변수들에 대해서는 타입을 미리 결정하고,  \r   타입이 정의되어 있지 않은 변수들에 대해서는 타입스크립트가 초기화된 값을 기준으로 알아서 타입을 추론한다\r - 코드 실행 전 검사를 통해 타입 안정성을 확보한다",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.5"
  }
}
